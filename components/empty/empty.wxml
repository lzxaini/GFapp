<view class="empty" style="background-color: {{bgColor}}">
  <t-empty>
    <image slot="image" class="image {{imageClass}}" src="{{image}}"></image>
    <view slot="description" class="description {{descClass}}">{{description}}</view>
  </t-empty>
</view>